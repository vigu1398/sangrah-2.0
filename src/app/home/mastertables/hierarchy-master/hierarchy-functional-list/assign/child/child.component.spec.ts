/* import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { ChildFunComponent } from './child.component';

describe('ChildFunComponent', () => {
  let component: ChildFunComponent;
  let fixture: ComponentFixture<ChildFunComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ChildFunComponent]
    })
      .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(ChildFunComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
 */